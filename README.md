# UARTv1.0
This will (primarily) recieve data from a host computer onto an FPGA board. 

## File Structure
/ -> UART_Rx Unit/
        -> SIPO Unit
        -> Baud Generator
        -> Parity Detector
  -> A way to send parallel data from terminal to USB
